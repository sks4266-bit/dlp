{
  "name": "api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787 --persist-to .wrangler/state",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply dlp_db --local",
    "seed:mcheyne:generate": "node src/scripts/generate_mcheyne_seed.mjs",
    "seed:mcheyne:local": "pnpm seed:mcheyne:generate && wrangler d1 migrations apply dlp_db --local"
  },
  "dependencies": {
    "hono": "^4.5.2",
    "hono-zod-openapi": "^0.18.4",
    "zod": "^3.23.8",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240821.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.72.0"
  }
}
